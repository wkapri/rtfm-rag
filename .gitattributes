# Normalize line endings to LF in the repo regardless of the contributor's OS/
# git config, so `git status` doesn't show every file as modified after a
# clone on a different platform.
* text=auto eol=lf

# Windows-specific scripts genuinely need CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
